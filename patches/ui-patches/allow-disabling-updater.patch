diff --git a/toolkit/mozapps/update/UpdateServiceStub.sys.mjs b/toolkit/mozapps/update/UpdateServiceStub.sys.mjs
index cd71c0ffc7..632d1b350f 100644
--- a/toolkit/mozapps/update/UpdateServiceStub.sys.mjs
+++ b/toolkit/mozapps/update/UpdateServiceStub.sys.mjs
@@ -196,11 +196,9 @@ export class UpdateServiceStub {
    * See nsIUpdateService.idl
    */
   get updateDisabledForTesting() {
-    return (
-      (Cu.isInAutomation ||
-        lazy.Marionette.running ||
-        lazy.RemoteAgent.running) &&
-      Services.prefs.getBoolPref(PREF_APP_UPDATE_DISABLEDFORTESTING, false)
+    return Services.prefs.getBoolPref(
+      PREF_APP_UPDATE_DISABLEDFORTESTING,
+      false
     );
   }
 
diff --git a/browser/components/preferences/config/about-firefox.mjs b/browser/components/preferences/config/about-firefox.mjs
index c28905c..e8d5e0f 100644
--- a/browser/components/preferences/config/about-firefox.mjs
+++ b/browser/components/preferences/config/about-firefox.mjs
@@ -20,6 +20,7 @@ const BACKGROUND_UPDATE_CHANGED_TOPIC =
 if (AppConstants.MOZ_UPDATER) {
   Preferences.addAll([
     { id: "app.update.disable_button.showUpdateHistory", type: "bool" },
+    { id: "app.update.disabledForTesting", type: "bool" },
   ]);
 
   if (AppConstants.NIGHTLY_BUILD) {
@@ -148,6 +149,19 @@ Preferences.addSetting({
   visible: () => !UpdatesHelpers.showUpdatesSettings,
 });
 
+Preferences.addSetting({
+  id: "updatesDisabledForTesting",
+  pref: AppConstants.MOZ_UPDATER
+    ? "app.update.disabledForTesting"
+    : undefined,
+});
+
+Preferences.addSetting({
+  id: "updatesDisabled",
+  deps: ["updatesDisabledForTesting"],
+  visible: () => UpdatesHelpers.showUpdatesSettings && deps.updatesDisabledForTesting.value,
+});
+
 Preferences.addSetting({
   id: "updateApp",
   visible: () => UpdatesHelpers.showUpdatesSettings,
@@ -289,12 +303,18 @@ Preferences.addSetting({
   },
 });
 
+const UPDATE_MODE_AUTO = "auto";
+const UPDATE_MODE_MANUAL = "manual";
+const UPDATE_MODE_DISABLED = "disabled";
+
 Preferences.addSetting(
   class extends Preferences.AsyncSetting {
     static id = "updateRadioGroup";
 
     defaultDisabled = true;
 
+    prefName = "app.update.disabledForTesting";
+
     /** @type {boolean | null} */
     _pendingValue = null;
 
@@ -314,8 +334,12 @@ Preferences.addSetting(
         return this._pendingValue;
       }
 
+      if (Services.prefs.getBoolpref(this.prefName, false)) {
+        return UPDATE_MODE_DISABLED;
+      }
+
       let value = await UpdateUtils.getAppUpdateAutoEnabled();
-      return value;
+      return value ? UPDATE_MODE_AUTO : UPDATE_MODE_MANUAL;
     }
 
     /**
@@ -329,7 +353,24 @@ Preferences.addSetting(
       );
 
       try {
-        await UpdateUtils.setAppUpdateAutoEnabled(value);
+        switch (value) {
+          case UPDATE_MODE_AUTO:
+            Services.prefs.setBoolpref(this.prefName, false);
+            await UpdateUtils.setAppUpdateAutoEnabled(true);
+            break;
+
+          case UPDATE_MODE_MANUAL:
+            Services.prefs.setBoolpref(this.prefName, false);
+            await UpdateUtils.setAppUpdateAutoEnabled(false);
+            break;
+
+          case UPDATE_MODE_DISABLED:
+            Services.prefs.setBoolpref(this.prefName, true);
+            break;
+
+          default:
+            throw new Error(`Unknown update mode: ${value}`);
+        }
 
         // Wait for a second to prevent the disable/enable causing the
         // UI text to flicker.
@@ -343,7 +384,7 @@ Preferences.addSetting(
 
       // If the value was changed to false the user should be given the option
       // to discard an update if there is one.
-      if (!value) {
+      if (value === UPDATE_MODE_MANUAL) {
         await UpdatesHelpers.checkUpdateInProgress();
       }
     }
@@ -390,8 +431,8 @@ Preferences.addSetting(
 
       // If auto updates are turned off, background updates checkbox should be unchecked
       let updateAutoEnabled = this._updateRadioSetting.value;
-      if (!updateAutoEnabled) {
-        return updateAutoEnabled;
+      if (updateAutoEnabled !== UPDATE_MODE_AUTO) {
+        return false;
       }
 
       let checked = await UpdateUtils.readUpdateConfigSetting(this.prefName);
@@ -422,7 +463,10 @@ Preferences.addSetting(
     }
 
     async disabled() {
-      return this._pendingValue !== null;
+      return (
+        this._pendingValue !== null ||
+        this._updateRadioSetting.value !== UPDATE_MODE_AUTO
+      );
     }
 
     setup() {
@@ -487,6 +531,14 @@ SettingGroupManager.registerGroups({
           role: "status",
         },
       },
+      {
+        id: "updatesDisabled",
+        l10nId: "update-application-updates-disabled",
+        control: "moz-message-bar",
+        controlAttrs: {
+          role: "warning",
+        },
+      },
       {
         id: "updateApp",
         control: "moz-box-group",
@@ -529,7 +581,7 @@ SettingGroupManager.registerGroups({
             options: [
               {
                 id: "autoDesktop",
-                value: true,
+                value: UPDATE_MODE_AUTO,
                 l10nId: "update-application-auto-2",
                 items: [
                   {
@@ -540,9 +592,14 @@ SettingGroupManager.registerGroups({
               },
               {
                 id: "manualDesktop",
-                value: false,
+                value: UPDATE_MODE_MANUAL,
                 l10nId: "update-application-check-choose-2",
               },
+              {
+                id: "disabledDesktop",
+                value: UPDATE_MODE_DISABLED,
+                l10nId: "update-application-disabled-choose-2",
+              },
             ],
           },
         ],
